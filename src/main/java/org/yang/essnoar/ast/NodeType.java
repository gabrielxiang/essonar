package org.yang.essnoar.ast;

public enum NodeType {
    ATTRIBUTE,
    ASSIGN,
    BINOP,
    BLOCK,
    BREAK,
    CALL,
    KEYWORD,
    CLASSDEF,
    CONTINUE,
    DELETE,
    FOR,
    FUNCTIONDEF,
    HANDLER, IFEXP, IF, NAME
}